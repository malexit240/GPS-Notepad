<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             xmlns:maps="clr-namespace:Xamarin.Forms.GoogleMaps;assembly=Xamarin.Forms.GoogleMaps" 
             xmlns:gpsnotepad="clr-namespace:GPSNotepad"
             xmlns:converters="clr-namespace:GPSNotepad.Converters"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="GPSNotepad.Views.AddPinPage"
             Style="{DynamicResource pageStyle}">
    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:MapClickedEventArgsConverter x:Key="mapClickedEventArgsConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>
    <StackLayout>
        <gpsnotepad:BindableMap x:Name="map" PinsSource="{Binding Pins}" MapSpan="{Binding Span}" >
            <gpsnotepad:BindableMap.Behaviors>
                <prism:EventToCommandBehavior EventName="MapClicked" 
                                              Command="{Binding OnMapClickedCommand}" 
                                              EventArgsConverter="{x:StaticResource mapClickedEventArgsConverter}"/>
                <prism:EventToCommandBehavior EventName="MapLoaded" Command="{Binding OnMapLoadedCommand}"/>
            </gpsnotepad:BindableMap.Behaviors>
        </gpsnotepad:BindableMap>
        <Entry Text="{Binding Path=PinViewModel.Name}" 
               Style="{DynamicResource entryStyle}"/>
        <Entry Text="{Binding Path=PinViewModel.Description}" 
               Style="{DynamicResource entryStyle}"/>
        <Entry Text="{Binding TextCoordinates}"
               Style="{DynamicResource entryStyle}"/>
        <Button Text="AddCommand"
                Command="{Binding AddEditPinCommand}"
                Style="{DynamicResource buttonStyle}"/>
    </StackLayout>
</ContentPage>