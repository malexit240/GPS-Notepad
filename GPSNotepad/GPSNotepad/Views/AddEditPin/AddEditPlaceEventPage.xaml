<?xml version="1.0" encoding="utf-8" ?>
<controls:BasePage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             xmlns:controls="clr-namespace:GPSNotepad.Controls"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="GPSNotepad.Views.AddEditPlaceEventPage"
             Title="{Binding TextResources[AddEditPlaceEventTitle]}"
             Style="{DynamicResource pageStyle}"
             NavigationPage.HasNavigationBar="False">

    <StackLayout>
        <controls:PageNavbar Title="Place events">
            <ImageButton BackgroundColor="Transparent" Grid.Column="5" Source="ic_save.png" Command="{Binding AddEditPlaceEventCommand}"/>
        </controls:PageNavbar>

        <controls:LabeledEntry LabelText="{Binding TextResources[Description]}"
                               EntryPlaceholderText="{Binding TextResources[Description]}"
                               EntryText="{Binding Description}"/>

        <DatePicker TextColor="{DynamicResource System_Black}" Date="{Binding Date}"/>
        <TimePicker TextColor="{DynamicResource System_Black}" Time="{Binding Time}"/>

    </StackLayout>
</controls:BasePage>