<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="GPSNotepad.Views.AddEditPlaceEventPage"
             Title="{Binding TextResources[AddEditPlaceEventTitle]}"
             Style="{DynamicResource pageStyle}">
    <ContentPage.ToolbarItems>
        <ToolbarItem IconImageSource="icon_save.png" Command="{Binding AddEditPlaceEventCommand}"/>
    </ContentPage.ToolbarItems>
    <StackLayout>
        <Entry Placeholder="{Binding TextResources[Description]}"
               Text="{Binding Description}" 
               Style="{DynamicResource entryStyle}"/>
        <DatePicker Date="{Binding Date}"/>
        <TimePicker Time="{Binding Time}"/>
    </StackLayout>
</ContentPage>